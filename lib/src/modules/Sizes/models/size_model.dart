class SizeModel {
  final String id;
  final String name;      // الإسم
  final String sizeValue; // الحجم المقاس

  SizeModel({
    required this.id,
    required this.name,
    required this.sizeValue,
  });
}